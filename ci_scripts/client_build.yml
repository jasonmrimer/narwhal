---
platform: linux

image_resource:
  type: docker-image
  source: {repository: jigsawdiux/ruby-golang-java-nodejs-chrome}

inputs:
  - name: repo

run:
  path: repo/ci_scripts/client_build.sh

params:
  TERM: -dumb

caches:
  - path: repo/node_modules
