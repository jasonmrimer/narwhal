---
platform: linux

image_resource:
  type: docker-image
  source: {repository: jigsawdiux/ruby-golang-java-nodejs-chrome}

inputs:
  - name: repo

run:
  path: repo/ci_scripts/client_test.sh

params:
  TERM: -dumb

caches:
  - path: repo/client/node_modules
