import * as React from 'react';
import styled from "styled-components";

interface Props {

}

export const FlightShiftPopup = (props: Props) => {
  return null;
}

export const StyledFlightShiftPopup = styled(FlightShiftPopup)`
  
  `;

